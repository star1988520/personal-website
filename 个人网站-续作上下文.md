# 个人网站 · 压缩续作上下文

> 后续直接在本目录续作。先看本文件，再按需查看 `index.html`、`styles.css`、`script.js`；不要回到旧日期目录，也不要重述历史。

## 一句话定位

“月桂之间”是一个无构建步骤的纯静态单页个人网站，视觉为复古电影、星空与神秘学气质；入口为 `index.html`，默认预览为 `http://127.0.0.1:4173/`。

## 页面与交互

- `#home`：视频首屏、个人介绍、三条抖音内容、星座运势、前端演示留言簿。
- `#videos`：奶油金古典胶片横幅；“干货 / 抽象”筛选、搜索、视频卡片及加载更多。
- `#about`：流星文字和双面书页。桌面为双页书，手机为图片在上、文字在下；翻页使用固定书脊分片弧面动画（桌面 24 片、手机 8 片），仅两种状态，可前进和返回。
- `#products`：顶部为“把想法，做出来”固定编排汇聚动画，下方为服务与精选卡片。
- `#contact`：联系信息和前端演示表单。

## 已锁定决定

- 保留现有复古纸张、奶油金、深棕、电影胶片和星光语言，不另起视觉方向。
- 产品页顶部每次进入播放约 4.8 秒：18 个淡墨念头在 0.6 秒内清晰并持续加速，约 2.6 秒后分别落向标题轮廓的固定位置并退墨；标题“把想法，做出来”从 1.5–3.8 秒同步积墨，英文和中文副文在 3.4–4.7 秒完成银盐显影。手机减少为 12 个词并按两行标题分配落点，减少动态效果时直接显示最终状态。
- 产品页英文眉题与中文副标题不再整块上移淡入：英文从中央向两端、中文以固定非连续顺序进行约 1 秒的银盐显影，字符只改变墨色、模糊和对比度，始终停在最终位置。
- 联系页顶部使用用户桌面 `羽毛笔.png` 抠出的白羽金笔素材；羽毛笔按旧动画的可见高度缩小并顺时针旋转约 26°。脚本实时测量六字位置，严格执行“羽毛笔完成当前字轨迹 → 当前字湿墨显现 → 抬笔前往下一字”，不再让笔和字同时出现。英文以淡墨开场，副标题在最后收笔后显影。
- 联系页“纸上余墨”不再按每两字分段触发：墨痕从 0.25–3.45 秒持续由中央向两侧延伸，并在 4.05 秒前淡至约 7% 后保留；副标题从 0.8–3.8 秒由极淡纸痕连续显影。二者与标题书写并行，不再突然跳档。
- 联系页完整羽毛笔演出压缩至约 4.1 秒：进场 280ms、每字书写 320ms、收笔显字 140ms、字间移动 60ms、离场 320ms；副标题与余墨阶段同步提速，严格先写后显的顺序不变。
- 联系页羽毛笔已由逐字等待重构为一条 3.65 秒连续主轨迹：脚本按实测字符位置生成落笔、字内、抬笔和离场路径，以距离分配时间并只动画 `translate3d` 与旋转；字符在各自收笔时间点后显现，羽毛笔不再停下等待。网页羽毛素材使用 224×400 Retina 版本。
- 视频横幅通铺、循环滚动，标题使用小型双层金线旧纸牌，两侧柔和棕色渐隐；下方视频列表不受横幅改动影响。
- 星座选择器使用 `assets/zodiac/` 的十二张图标，按钮、运势圆环、名称及内容同步更新。
- 首页肖像区已使用两段 3:4 视频：默认播放 `assets/home-portrait-before.mp4`，点击“愿世界和平”切换到 `assets/home-portrait-after.mp4`，再次点击返回；继续沿用 `idle / peaceful` 状态。两段视频不再使用原生 `loop`，循环前以对应首帧海报轻柔遮帧，待第 0 秒可绘制后恢复播放，避免重新解码时闪烁。
- 首页“关于我的更多信息”按钮固定为 300 × 62px，直接使用无缝预合成的 `about-button-default@2x.png` 与 `about-button-selected@2x.png`；hover、focus、active 只切换完整背景图，文字和箭头不移动。
- `script.js` 中的 `videoItems` 是视频数据源；“干货”有三条真实抖音内容，“抽象”现有一条内容，封面仍沿用站内抽象画面。
- 表单仅演示，不发送数据；素材不随意删除。

## 当前状态（2026-07-13）

- 2026-07-05 全站设计审查提出的 P0/P1 收尾已写入 `styles.css`：全局字体与间距变量、页面/区块/卡片标题层级、800px 布局断点、650px 手机字号与紧凑间距均已统一。
- 视频页末尾标题、关于页“近日生活 / 斯达星迹”、产品卡标题、联系页次级标题及平板单栏间距已按审查建议降级或收敛；特色动画、文案、图片和构图保持不变。
- 390 / 768 / 1440px 全站复测基线仍有效：页面 hash 与激活栏目一致，三档均无横向溢出；浏览器控制台无网页错误。
- 上个置顶对话中断的首页按钮任务已收尾：两张无缝按钮图已接入，桌面与手机均为 300 × 62px；手机端补足按钮与区块底边的净距，避免底边线从按钮右侧穿出。
- 首页“写入留言簿”提交按钮已改为 127 × 48px 五切片图片按钮：常规态使用未选中素材，hover、focus-visible、active 切换选中素材；四角与中央图案保持比例，仅两侧横向伸缩，且不影响通用 `.button`。
- 视频横幅此前遗留的 1440 / 768 / 390px 浏览器确认已完成；三档均无横向溢出，横幅背景与 C2 画框尺寸符合既定方案，控制台无错误。
- 关于页翻书设计质检仍为通过状态；当前没有确认的未完成开发项。后续若收到浏览器批注，以批注区域和用户文字为准继续修改。

## 每次续作的最短流程

1. 在本目录修改，不回写旧目录。
2. 启动 `python3 -m http.server 4173 --bind 127.0.0.1`。
3. 在内置浏览器打开目标 hash，检查标题、目标内容、交互与控制台。
4. 只把新的重要决定或明确待办补到本文件，保持精简。

常用地址：

- 首页：`http://127.0.0.1:4173/#home`
- 视频：`http://127.0.0.1:4173/#videos`
- 关于我：`http://127.0.0.1:4173/#about`
- 产品：`http://127.0.0.1:4173/#products`
- 联系：`http://127.0.0.1:4173/#contact`

## 新对话最短提示

打开桌面“个人网站”，先读《个人网站-续作上下文.md》，直接续作；完成后启动 4173 预览并在内置浏览器验证。
